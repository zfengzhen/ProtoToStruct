MESSAGE(STATUS "CMAKE_CURRENT_SOURCE_DIR" ${CMAKE_CURRENT_SOURCE_DIR})

FILE(GLOB_RECURSE PROTO_LIST "*.proto")

FOREACH(proto ${PROTO_LIST})
    EXECUTE_PROCESS(COMMAND ${PROTOC}
        -I=${PROTOCOL_DIR}/proto/
        -I=${GOOGLE_INCLUDE}
        --cpp_out=${PROTOCOL_DIR}/proto_gen_cpp ${proto})
ENDFOREACH(proto)
